# Build output
*.o
*.exe
find5

# Runtime DLLs — copied next to the exe from ../SOOB-Core/vendor* by the
# Windows build scripts, so this repo doesn't carry its own ~4.7MB copy.
SDL.dll
OpenAL32.dll
libwinpthread-1.dll

# Not usable: a 64-bit Creative Labs router DLL, not OpenAL Soft. Kept out of
# the way rather than shipped. See ../SOOB-Core/vendor_win10/README.md.
OpenAL32-win10*.dll
build/
raw/obj/
dist/

# Engine Lua modules copied from ../SOOB-Core/ at build time.
# Source of truth lives in SOOB-Core; this dir is regenerated on each build.
scripts/engine/

# Temp/log files
output.txt
stdout.txt
stderr.txt
ai-on-itself.txt
find5.log

# F12 frame dumps
find5_shot_*.bmp

# Scratch screenshots
screenshot.png

# Downloaded installers
ms3d184setup.exe

# Win10 build - portable MinGW (download separately, ~250MB)
vendor_win10/mingw32/
vendor_win10/SDL.dll
libwinpthread-1.dll
